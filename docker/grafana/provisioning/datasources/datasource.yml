# Grafana datasource provisioning — wires Prometheus as the default source.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: aegis-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      httpMethod: POST
      timeInterval: 10s
